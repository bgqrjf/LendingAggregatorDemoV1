// SPDX-License-Identifier: UNLICENSED
pragma solidity >= 0.6.0;

import "@openzeppelin/contracts/token/ERC20/IERC20.sol";

library UserAssetMap{
    uint public constant MAX_NUMBER_OF_COLLATERAL_ASSETS = 128;

    function isCoolateralAsset(uint _userAssetsBitMap, uint _i) internal pure returns (bool){

    }

    function isDebtAsset(uint _userAssetsBitMap, uint _i) internal pure returns (bool){

    }
}